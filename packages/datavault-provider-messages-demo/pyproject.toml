[project]
name = "datavault-provider-messages-demo"
version = "0.1.0"
description = "Example messages provider plugin for Data Vault"
authors = [
  { name = "Gabriel Montague", email = "montaguegabe@users.noreply.github.com" },
]
requires-python = ">=3.12"
dependencies = [
  "datavault-plugin-sdk",
]

[project.entry-points."datavault.data_providers"]
messages_demo = "datavault_provider_messages_demo:get_plugin"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/datavault_provider_messages_demo"]
